# 验证报告

验证日期：2026-08-28。

## 初始版本的内容与结构

- 12个技能全部通过本机 `skill-creator/scripts/quick_validate.py` 校验。
- 另外检查了必填字段非空、名称与目录一致、名称限制、元数据值类型、相对引用不越出技能目录，以及引用文件存在。
- 每个 `SKILL.md` 为34—52行，三个辅助引用文件均可从对应入口找到。没有技能依赖另一个技能目录中的文件。
- YAML 校验使用 Python 3.12 和任务目录内的 PyYAML 6.0.2；未改动全局 Python 依赖。技能本身没有 Python 依赖。

这不是 Anthropic 的认证；未运行 `skills-ref`，采用的是本机校验器及对官方规范的额外检查。

## 当前工作区的自动校验

仓库已提供 [校验器](scripts/validate_skills.py) 和 [回归测试](tests/test_validate_skills.py)，不再依赖维护者机器上的技能目录。校验必填字段、YAML 重复键及类型、名称与目录、相对引用范围和文件存在性；记录被测文件及校验器 SHA-256、UTC 时间、Python/PyYAML 版本。详细运行方法与语法限制见 [验证与复跑](validation/README.md)。

本轮使用 Python 3.14 和任务临时目录中的 PyYAML 6.0.3，没有安装到全局环境。12个技能通过结构检查及本机 `skill-creator/scripts/quick_validate.py` 交叉校验。逐项文件指纹与实际环境见 [结构校验结果](validation/structural-validation.json)。

校验器经过先失败、后实现的回归测试，24/24通过，覆盖错误 YAML、重复键、空字段、缺失/越界引用、从其他目录运行、内容变化导致快照过期、输出覆盖保护及行为记录完整性。Python 语法编译通过；没有独立的 lint、格式化、类型检查或构建配置，不把本轮验证称为这些检查已通过。

## 本轮多轮与反例验证

[用例](validation/behavior-cases.json) 保存逐轮合成输入与评分准则；[结果](validation/behavior-results.json) 保存7个场景、15轮实际回答、来源文件哈希、主代理审阅及限制。作答代理不读取评分准则，主代理逐条回读实际回答再评分，不代表已经有人类复核。

| 场景 | 实际轮数 | 观察结果 |
| --- | --- | --- |
| 苏格拉底提问 | 4 | 根据新事实更新理解；接受停止，重述并等待确认；确认后才建议 |
| 优势发现 | 3 | 跳过敏感主题，考虑相反经历和外部框架，输出暂定说明与可反驳实验 |
| 人生设计 | 3 | 接受用户要求提前出暂定三路，不编评分；用户表达偏好后给两小时内的零预算原型 |
| 双向钢人 | 2 | 首轮等待关键证据，次轮给条件式判断与反转条件 |
| 事实核查 | 1 | 在禁网且工具不可用时，仅分析合成材料，区分样本、统计量、因果与价值 |
| 最小实验 | 1 | 只验证一个必要未知，不凑假设，不虚构基线或声称已执行 |
| 跨领域触发 | 1 | 三个描述级正负例分类符合边界，不把普通调试或明确排除类比的请求判为触发 |

以上7个场景的限定准则均通过主代理审阅。最小实验的修改前同例也遵守了用户要求，本轮消除的是固定数量的指令歧义，不能声称复现过该行为失败；其复测沿用同一代理上下文，已在记录中披露。其他场景使用独立对话，后续用户输入逐轮发送，不是一次性模拟整段对话。

具体模型标识和版本没有通过执行接口提供，记录为 `null`，不能声称跨模型验证。`--check-behavior` 只验证记录完整性及来源是否过期，不自动判断模型回答的质量。

## 初始版本的行为抽查（历史）

两个独立上下文的测试代理读取技能后，分别对以下六个场景生成了实际回答；主代理回读回答检查，没有仅依赖“通过”的摘要。

| 技能 | 场景 | 已观察的结果 | 验证边界 |
| --- | --- | --- | --- |
| 苏格拉底提问 | 团队效率下降，怀疑站会太短 | 先问一个具体事件，不提前给会议方案 | 仅首轮，未验证六轮收束和确认节点 |
| 隐藏天赋 | 擅长整理但完成后很累 | 区分能力与能量，先问经历，不直接判定天赋 | 仅首轮，未生成完整优势报告 |
| 人生设计 | 疲惫但不愿作不可逆决定或负面想象 | 接受边界，只问一个现状问题 | 仅首轮，未完整生成三条路径 |
| 事实核查 | 无课程名称或数据，禁止联网，声称收入涨50% | 标记证据不足，区分统计、因果、价值，不声称完成外部核查 | 只检查逻辑与缺失信息，无外部事实验证 |
| 双向钢人 | 本月上线或延期，偶发同步故障未查明 | 条件式呈现双方依据，提出一个关键问题，未提前推荐 | 仅第一轮，未验证收到回答后的最终判断 |
| 最小实验 | 想开咖啡店，七天、最多500元、不联系陌生人、不影响工作 | 给出受限的可逆实验，明确不能证明需求或盈利，也不能据此辞职 | 验证的是实验设计，没有实际开展七天实验 |

原始生成回答和检查记录：

- [交互技能实际回答](validation/interactive-forward-test.md)
- [分析技能实际回答及复测](validation/analysis-forward-test.md)

抽查后收窄补充了事实核查的禁网分支，以及最小实验在关键假设无法测试时的替代范围说明；对这两个相同场景进行了复测。
